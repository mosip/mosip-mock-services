# Loaded when spring.profiles.active includes local, before application-local.properties.
# Stops the config client from calling the Cloud Config default URI.
spring.cloud.config.enabled=false
spring.cloud.config.fail-fast=false
spring.config.import=optional:configserver:
